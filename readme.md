web build:
```bash
odin run ./libs/karl2d/build_web/ -- ./source
```

to run web build:
```bash
# run simple webserver to serve the files
python3 -m http.server
# launch web browser on localhost:8000
firefox localhost:8000
```
